# gloo-beep

A core gloo library for testing.
Not part of the documented core libraries.

## Usage

Load this library in a gloo script:

```gloo
> load lib beep
```

## Verbs

- `beep` (shortcut `b`) — play a standard system beep sound
