# #
# # postgres tests
# #
# # No Postgres server is reachable on this machine, and even if there
# # were, nothing here connects to a real one - same discipline as
# # gloo-mysql. Structure only: typename, messages, default children.
# #
# tests [can] :
#   pg [can] :

#     on_load [script] :
#       load lib db
#       load lib pg

#     pg_object [test] :
#       description [string] : The postgres object exists
#       on_test [script] :
#         exists? object postgres
#         assert "postgres object should exist"

#     pg_short_typename [test] :
#       description [string] : the pg short typename is registered
#       on_test [script] :
#         exists? object pg
#         assert "expected the pg short typename to be registered"

#     pg_messages [test] :
#       description [string] : postgres responds to verify and unload
#       on_test [script] :
#         create tests.pg.db as postgres

#         check ^^.db for responds_to? ('verify')
#         assert 'expected postgres to be able to verify'

#         check ^^.db for responds_to? ('unload')
#         assert 'expected postgres to be able to unload'

#     pg_default_children [test] :
#       description [string] : postgres default children are host, database, username, and password
#       on_test [script] :
#         create tests.pg.db2 as postgres

#         exists? instance tests.pg.db2.host
#         assert "expected postgres to have a host child"

#         exists? instance tests.pg.db2.database
#         assert "expected postgres to have a database child"

#         exists? instance tests.pg.db2.username
#         assert "expected postgres to have a username child"

#         exists? instance tests.pg.db2.password
#         assert "expected postgres to have a password child"
